export interface IStepper {
    title: string;
    subtitle?: string;
    description?: string;
    icon?: string;
}

export interface IStepContent {
    title: string;
    subtitle: string;
    image: string
}